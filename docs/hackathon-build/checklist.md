# Build Checklist

> Locked `$build-project` contract. The participant accepted this workload on 2026-08-31; starting `$build-project` is the explicit implementation `GO` for this scoped checklist only. It is not permission to publish, spend money, use paid APIs, or submit to Devpost.

## Build Preferences

- **Plan design:** Handed off to Codex, based on the existing scope, PRD, spec, learner profile, and adversarial reviews.
- **Build mode:** Autonomous. This locks when building starts.
- **Comprehension checks:** N/A. Explain outcomes and decisions in plain language, but do not stop for code quizzes.
- **Git:** No automatic commits while the current uncommitted planning work remains user-owned. Preserve unrelated changes and do not rewrite history. Commit only if the participant explicitly authorizes it later.
- **Verification:** Yes—automated proof after every item, with only three look-at-it pauses: after item 4 (causal `GO / PIVOT / KILL`), item 8 (no-Site-tools core loop plus publication-permission gate), and item 11 (release/demo proof).
- **Check-in cadence:** Speed-run between those pauses. Report only a failed gate, a material scope cut, a permission boundary, or completion.
- **Cost boundary:** No paid service, API, model credit, or account upgrade without explicit permission. Production runtime has no external API call.
- **Estimate meaning:** The 15–30 minute labels are bounded active-work slices, not a promise that retries or external processing will fit inside their sum. Wall-clock gates and the cut rules decide scope.
- **Publication boundary:** Public repository creation, commits needed for release, live deployment, public YouTube upload/visibility, and Devpost writes require explicit permission at the relevant gate. One named publication approval may cover repository + live site + YouTube; Devpost submission remains a separate final confirmation.
- **Human response windows:** Request the named publication approval immediately after item 8, targeting a reply by 2026-09-01 06:00 JST. Request the separate final Devpost-submit confirmation during `$submit-project`, targeting a reply by 2026-09-04 01:00 JST.

## Submission Anchor

The wow moment is one continuous proof: confirming repair changes a canon-dependent gate, while Gen's theft belief and its multi-hop rumor provenance remain; a premature Apply is refused, then only the two operations approved in the current page-review state commit. The submission must show all four defensible mechanisms together: explicit NPC stance, stored hop/refusal/distortion provenance, registered canon-vs-belief condition projection, and page-owned WebMCP operation review.

## Schedule And Cut Rules

- The planning-lock target slipped by about one hour on 2026-08-30; no feature expansion is allowed to consume the remaining buffer.
- Target items 1–4 by 2026-08-31 06:00 JST; hard causal fallback remains 2026-09-01 23:00 JST.
- Items 5–8 must make the no-Site-tools core loop green by 2026-08-31 23:00 JST.
- Items 9–10 must produce the complete static product by 2026-09-02 20:00 JST.
- Item 11 targets a verified local demo recording by 2026-09-03 20:00 JST. Item 12 must verify the public video and hand off by the 2026-09-04 02:00 JST internal cutoff.
- Already out of the MVP: semantic search, second sample, and JSON import/export. If time slips, cut decorative polish next. Never cut verified provenance, typed constraint projection, fail-closed review/apply, verified local persistence/reset, the complete no-Site-tools path, or required submission artifacts.

## Checklist

- [x] **1. Establish the typed static shell and preserve Day 0 evidence (20 min)**
  Spec ref: `spec.md > Stack`; `spec.md > File Structure`; `spec.md > Data Flow > Startup and recovery`
  What to build: Move the current diagnostic page to `probe/index.html`; create the Vite/strict-TypeScript/Vitest shell, exact pinned dev dependencies and lockfile, relative build configuration, `.nojekyll`, minimal semantic product entry, narrow `model-context.d.ts`, and one smoke test. Preserve the Day 0 JSON plus the Codex in-app-browser recheck showing a real WebMCP call, a complete 128 KiB result tail, and `requestUserInteraction` as an undocumented zero-arity function that rejected immediately as unsupported when invoked. Keep production dependencies, remote assets, server code, and external APIs at zero.
  Acceptance: The root is unmistakably the product shell rather than the five-tool diagnostic probe; the probe/evidence remains runnable and correctly labels the measured route as Codex in-app Browser rather than a ChatGPT Work/manual run; `dist/` uses relative assets; a browser without Site tools reaches an honest no-Site-tools shell; no paid or server dependency appears. Product correctness does not invoke or depend on `requestUserInteraction`.
  Verify: Run `npm run build` and `npm test -- tests/toolchain.test.ts`; serve `dist/` statically and confirm the shell opens without console errors.

- [x] **2. Port the deterministic scoring and provenance core (30 min)**
  Spec ref: `spec.md > Domain Model > Memory and rumor provenance`; `spec.md > Domain Model > Belief`; `spec.md > Components And Responsibilities > Scoring And Belief Evaluator`
  What to build: Adapt only the MIT-licensed pure scoring, support-count, contradiction-group, and provenance functions from Rumor Memory Village. Add the JSON/date adapter, six-decimal canonical rounding, stable canonical JSON/SHA-256 helpers, full domain types and manual validators. Exclude SQL, Next, React, AWS, vector, cookie/world-fork, and managed-MCP code; record exact attribution.
  Acceptance: Existing pure behavior remains deterministic under an injected clock; canon is not an evaluator input; relationship, memory, transfer, belief, and reference validation has no silent defaults; prior reusable tests plus adapter/digest mutations pass.
  Verify: Run `npm test -- tests/scoring.test.ts tests/provenance.test.ts tests/domain-validation.test.ts` and `npm run build`.

- [x] **3. Generate and lock the honest warehouse fixture (30 min)**
  Spec ref: `spec.md > Warehouse Fixture Contract`; `prd.md > Warehouse Demo Fixture Requirements`
  What to build: Create `build-fixture.ts` and compact checked-in JSON using the production evaluator/validator. Generate the five named characters plus enough secondary actors for paging, complete directed relationships, two competing claims, explicit canon, accepted/rejected transfer union records, beliefs, and four registered conditions. Add `--check` regeneration without rewriting the fixture.
  Acceptance: The checked-in fixture has more than 10 default-claim beliefs, depth ≥3 accepted provenance, ≥2 independent roots, a threshold-proven low-trust refusal that creates no memory, visible wording/confidence distortion, exact transfer↔memory linkage, and one explicit recomputed belief per incident actor. One actor holds evidence for both mutually exclusive claims, producing a non-zero opposing score and a stance that changes when the relation is removed; no-evidence actors remain `unknown`. Regeneration is byte/digest identical; exact counts are test-derived rather than prose guesses.
  Verify: Run `npm run fixture -- --check` and `npm test -- tests/fixture.test.ts tests/provenance.test.ts tests/scoring.test.ts`.

- [x] **4. Prove registered-condition causality before UI work (30 min)**
  Spec ref: `spec.md > Domain Model > Registered game constraints`; `spec.md > Tests And Verification > Constraint causality`
  What to build: Implement typed canon/belief dependency evaluation, invalid/unresolved rows, layer-qualified read/write intersection, isolated-operation/full-patch projection, and separate transition/verdict axes. Lock the exact four warehouse rows and aggregation semantics (`evaluated`, `unresolved`, `causallyAffected`, `changed/preserved`, `violated before→after`).
  Acceptance: Initial violations are 1; canon-only repair leaves violations at 1 while `traveller_can_stay` is `changed+satisfied`, both belief lines are `preserved+satisfied`, and wrong-layer `warehouse_dispute` is `changed+violated`; the condition correction yields 0 violations with `definitionChanged:true`. Canon-only projection leaves belief, memory, transfer, relationship, provenance, and belief-condition results byte/deep equal. **Pause 1:** present the exact table and a frank `GO / PIVOT / KILL`. If this proof fails by the hard stop, remove the game-condition blast-radius claim and re-scope before any UI polish.
  Verify: Run `npm test -- tests/constraints.test.ts tests/fixture.test.ts` including all four Boolean quadrants, missing-reference cases, same-claim canon/belief mutation-kill tests, and exact fixture snapshots.

- [x] **5. Build the pure fail-closed patch planner (30 min)**
  Spec ref: `spec.md > Patch And Review State Machine`; `spec.md > Domain Model > Patch, operation, review, and receipt`
  What to build: Implement exact kind-specific operations (`resolve_canon_relation`, `replace_constraint_dependency`, `archive_memory`), canonical operation/patch fingerprints, page-owned revision-bound reviews, provisional/selected/final projections, one-time derived belief recomputation, receipt identity, and async plan digests. Keep the planner storage/DOM/clock-free.
  Acceptance: Stage/review/preview never mutate committed state; pending, stale patch, stale operation, duplicate write key, receipt conflict, missing reviewed digest, preview mismatch, all-rejected no-op, and duplicate Apply are distinct deterministic outcomes. Each operation can be approved alone and remains valid; archive never deletes or breaks provenance.
  Verify: Run `npm test -- tests/patches.test.ts tests/constraints.test.ts`, including exact revision increments, old-approval invalidation, canon-only belief preservation, independently approved archive, idempotence, and tamper tests.

- [x] **6. Add the single store and verified persistence/apply shell (30 min)**
  Spec ref: `spec.md > Components And Responsibilities > App Store And Persistence Shell`; `spec.md > Patch And Review State Machine > Apply algorithm`
  What to build: Implement one `AppState` store and shared commands; bind page-UI decisions to operation fingerprints and patch revision; record `createdVia`/`decidedVia` as routes rather than actor identity; compute the final digest in the last-decision store transition; commit the planner candidate through pre-validate → single-key write → readback/digest verification → in-memory swap. Add rollback verification, `writes_disabled` recovery, bounded audit, reload restore, receipts, and explicit fixture reset.
  Acceptance: Pending/stale/mismatched attempts change no world data; an approved partial patch advances world revision once and only approved entity revisions; rejected archive leaves Gen's memory byte-equivalent; failed storage restores the prior raw state or disables writes visibly; reload restores same-origin active review without manufacturing authority or claiming actor authentication.
  Verify: Run `npm test -- tests/patches.test.ts tests/persistence.test.ts tests/store.test.ts`; exercise quota/write/readback/rollback failures through an injected `StorageLike` fake.

- [x] **7. Implement bounded selectors and shared application commands (25 min)**
  Spec ref: `spec.md > Components And Responsibilities > Search Selector`; `spec.md > Data Flow > Search and synchronized page state`; `spec.md > Data Flow > Provenance trace`
  What to build: Add deterministic Japanese/English keyword search, actor/claim/stance/source/condition filters, stable cursor pagination, first-screen aggregate selectors, bounded provenance traces with rejected branch attempts, constraint view selection, and shared search/trace/check/suggest/apply commands for both UI and tools.
  Acceptance: Default `sc-stole` stance totals equal unique incident actors; page size defaults to 10 and maxes at 25; zero/one/many-result states are explicit; cursors bind world revision, filter signature, and informative-first ordering; held-evidence rows precede direct incoming refusals and never-heard rows; accepted depth, belief stance, and rejected transfer attempts are never conflated; normal serialized results remain within 12 KiB.
  Verify: Run `npm test -- tests/search.test.ts tests/provenance.test.ts tests/commands.test.ts`; snapshot zero/one/paged searches, forged/stale cursors, and bounded trace output.

- [x] **8. Deliver the complete no-Site-tools core workflow (30 min)**
  Spec ref: `spec.md > Components And Responsibilities > Human Interface`; `prd.md > Epic 1` through `Epic 7`
  What to build: Render the environment banner, metric strip, visible filters, paged belief table, canon-vs-belief detail, bounded trace, four-row condition projection, Suggestions cards, operation-level visible page review, Apply/recheck, audit/receipt, reload, and reset. Use semantic HTML, keyboard-visible controls, event delegation, and `textContent` for fixture/stored strings; no force graph or decorative detour.
  Acceptance: In a normal browser with no Site tools, a person can search → trace → stage three operations → see premature Apply refusal → approve canon/condition and reject archive → pause on violations `1→1` while the broken row swaps from `traveller_can_stay` to `warehouse_dispute` → Apply after the definition repair → see the complete `1→1→0` sequence, Gen belief/memory preserved, roots delta 0, and revision +1. The fresh first page prioritizes informative rows, shows Tatsu's evidence-backed rejected theft stance and its held opposing evidence/scores, distinguishes Aya's refused incoming rumor from `never heard`, and pluralizes memory counts correctly. **Pause 2:** show the actual page, confirm the core proof is understandable without reading code, and request one explicit named approval covering a public source repository, live deployment, release commits, and public YouTube upload. If permission is granted, item 9 publishes the green core first; if withheld, make no public write and continue locally. If the core is red at 2026-08-31 23:00 JST, cut polish and fix only this loop.
  Verify: Run `npm run build`, serve `dist/`, complete the clean-fixture no-Site-tools path, reload at pending review and after commit, and record the visible before/after values in build notes.

- [x] **9. Publish the authorized core early, then register and evaluate five Site tools (30 min active work)**
  Spec ref: `spec.md > Site Tool Contracts`; `spec.md > Components And Responsibilities > Site Tool Adapter`; `spec.md > Data Flow > Startup and recovery`
  What to build: If item 8's named publication permission was granted, create the public source repository and initial Pages deployment from the green core first, then verify the URL in a fresh signed-out profile; otherwise make no public write. Define the five exact descriptions, expanded schemas and annotations; register all descriptors synchronously before the top-level module's first `await`; make handlers await shared `storeReady` and call only shared commands. Include plain JSON replies, page-state synchronization, field-level validation, authority/pending metadata, `untrustedContentHint:true`, and no review-decision/reset/arbitrary-operation input. Preserve the diagnostic evidence that the runtime's undocumented `requestUserInteraction` option rejected as unsupported; never invoke or depend on it in product code. Run the real Sol/Terra path and 10–12 prompt natural-language eval while descriptions and schemas are still cheap to correct, then redeploy the updated build if publication was authorized.
  Acceptance: Authorized source/live URLs exist early and open without privileged state, or remain untouched if permission was withheld. Exactly five tools discover on a fresh load without relying on `ontoolchange`; an invocation during initialization resolves after readiness; read-only calls affect only view+inspection audit; suggestion cannot overwrite an open patch or commit; Apply cannot create a decision; prompt-like fixture labels remain quoted data. Page and tool routes produce equal state for the same starting state and review decisions. Browser invocation safety review and page-owned operation decisions remain explicitly separate. A browser-control red-team may operate a visible review control, but the audit records `page-ui` and the product makes no human-identity claim; a Site-tool call alone cannot create a decision. All eval expectations pass after recorded fixes/retries, with zero dangerous wrong-tool calls.
  Verify: Run `npm test -- tests/tools.test.ts tests/commands.test.ts` and `npm run build`; snapshot exact descriptions/schemas/annotations, fresh-load registration, initialization failure, result budgets, prompt injection, stale request, open-patch refusal, route equivalence, and a test proving Site-tool inputs cannot mint review decisions. In the real Sol/Terra runtime, run and save the 10–12 prompt eval matrix with expected tool/result code, observed result, failure, fix, and rerun outcome; verify the live URL signed out if authorized.

- [ ] **10. Harden and validate the release candidate (30 min)**
  Spec ref: `spec.md > Validation, Security, And Performance`; `spec.md > Tests And Verification > Manual/runtime gates`; `spec.md > Deployment`
  What to build: Freeze features; add the restrictive CSP, accessible labels/focus/status messages, recovery banner, reset confirmation, bounded audit/cursors/DOM, and production measurements. Capture exact fixture counts, result sizes, render/search/preview timing, compressed critical-path size, and screenshot paths. Update the authorized live build if one exists; do not add features or make an unauthorized public write.
  Acceptance: All tests/build pass; the keyboard-only and no-Site-tools workflows remain complete; compressed critical path ≤300 KiB, first render <500 ms, search/preview <100 ms on the development machine, and normal tool replies ≤12 KiB. License, attribution, README, evidence links, CSP, recovery states, and visible accessibility labels are release-ready. No new feature enters after this gate.
  Verify: Run the full `npm test` and `npm run build`; inspect the production bundle and CSP; run keyboard-only and no-Site-tools smoke tests; verify license/attribution/README/evidence links; measure and save the production budgets; rerun the Apply refusal/approval path once on the release artifact.

- [ ] **11. Publish the live build and record the required demo (30 min active work)**
  Spec ref: `spec.md > Deployment`; `spec.md > Demo And Submission Flow`; `prd.md > Demo And Submission Acceptance`
  What to build: If item 8's named publication permission was granted, publish the hardened release to the already tested source/live path and verify it in a fresh signed-out profile. Run the real Sol/Terra five-tool path, then complete two fresh-state 2:15 rehearsals with different natural-language phrasings and clear English explanatory audio; record both takes and retain the strongest simple one. If permission was withheld, rehearse/record locally without uploading or creating public resources.
  Acceptance: Authorized source/live URLs work without privileged state; all five tools discover and the real-runtime path completes with zero mutation without a complete current decision set; both rehearsals finish ≤2:15; the retained take is continuous, truthful, under 2:30, and intelligible. It shows the live WebMCP path plus explicit page-review boundary, exposes no secrets, and makes no unsupported human-authentication, scale, or engine-wide claim. The spoken beats emphasize violations `1→1→0`, pausing on “the count did not move, but a different condition broke,” and Gen's preserved belief; refusal and approved-only commit remain visibly on screen without equal narration weight. **Pause 3:** show the release proof and strongest take to the participant before public video upload.
  Verify: Open the live URL in a fresh profile if authorized, run five-tool discovery/apply, save both rehearsal outcomes, inspect the retained duration/audio locally, and compare every spoken claim with the saved evidence matrix.

- [ ] **12. Publish the required demo video and prepare the Devpost handoff (30 min active work)**
  Spec ref: `spec.md > Demo And Submission Flow`; `prd.md > Demo And Submission Acceptance`; `scope.md > Submission Story`
  What to build: Select and minimally trim/export the retained take; only under item 8's explicit publication permission, upload it as a **Public YouTube** video and verify signed-out playback. Assemble a bounded handoff file containing the verified one-line pitch, four-part differentiation and PlotLens boundary, the decision-provenance line, exact fixture/bundle/eval numbers, limitation wording (“registered conditions only”), screenshot paths, verified YouTube/source/live URLs, run/test instructions, MIT attribution, AI-usage/build-process notes, and unresolved blockers. Do not submit or otherwise mutate Devpost in this item.
  Acceptance: The public YouTube video is under 3:00, plays without sign-in, has clear explanatory audio covering the product and its WebMCP use, and exposes no secrets or unsupported claims. Every public claim maps to a test or captured runtime artifact; required URLs are verified or explicitly blocked; the participant has everything needed to run `$prepare-submission`.
  Verify: Check media duration/audio locally, play the complete public video signed out if authorized, review the handoff against the official submission requirements, and confirm the next command after the completed build is `$prepare-submission`.
