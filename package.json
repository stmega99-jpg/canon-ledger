{
  "name": "canon-ledger",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8787",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8801",
    "test": "vitest run",
    "test:keyboard": "node scripts/keyboard-smoke.mjs",
    "test:watch": "vitest",
    "fixture": "node --experimental-strip-types scripts/build-fixture.ts"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
